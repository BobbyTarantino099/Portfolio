# Dependencias y artefactos de construccion
node_modules/
dist/
.astro/

# El HTML con las fuentes incrustadas que build_cv.py imprime; se regenera.
scripts/cv/cv.render.html

# Entorno
.env
.env.production

# Basura de SO / editores
.DS_Store
Thumbs.db
*.log

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
