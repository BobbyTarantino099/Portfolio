# Dependencias y artefactos de construccion
node_modules/
dist/
.astro/

# Entorno
.env
.env.production

# Basura de SO / editores
.DS_Store
Thumbs.db
*.log

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
